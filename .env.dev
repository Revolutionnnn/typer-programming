# ─── Development Environment ───

# Server
PORT=8080
API_PORT=8080
WEB_PORT=4200

# Authentication (safe for dev only)
JWT_SECRET=dev-secret-key-change-in-production-at-least-32-chars-long

# Cookie – set to false for local dev without HTTPS
COOKIE_SECURE=false

# PostgreSQL
POSTGRES_USER=typer
POSTGRES_PASSWORD=typer
POSTGRES_DB=typer
DB_PORT=5432
DATABASE_URL=postgres://typer:typer@db:5432/typer?sslmode=disable

# Content
CONTENT_DIR=/app/content

# CORS – comma-separated origins
ALLOWED_ORIGINS=http://localhost:4200,http://localhost:3000
